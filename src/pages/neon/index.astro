---
import Layout from '../../layouts/Layout.astro';
import SiteRenderer from '@quorlansoftware/astro-web-components/SiteRenderer.astro';
import siteConfig from '../../content/site.json';
const page = siteConfig.pages.find((item) => item.id === 'neon');
---

<Layout title={page.metadata.title} description={page.metadata.description} theme="neon" locale="es" canonical="/portfolio/neon/" alternates={[{ locale: 'es', href: '/portfolio/neon/' }, { locale: 'en', href: '/portfolio/en/neon/' }]}>
  <SiteRenderer config={siteConfig} pageId="neon" locale="es" />
</Layout>
